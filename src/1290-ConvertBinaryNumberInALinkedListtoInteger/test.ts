// To run test use this:
// yarn jest src/1290-ConvertBinaryNumberInALinkedListtoInteger --watch

import { getDecimalValue } from './index'

test('Convert Binary Number in a Linked List to Integer', () => {
  expect(getDecimalValue).toBeDefined();
});
// TODO: Write tests
// test('head = [1,0,1]', () => {
//     expect(getDecimalValue([1,0,1])).toEqual(5)
// });

// test('head = [0]', () => {
//   expect(getDecimalValue([0])).toEqual(0)
// });
